import { ValidateFormControlsDirective } from './validate-form-controls.directive';

describe('ValidateFormControlsDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateFormControlsDirective();
    expect(directive).toBeTruthy();
  });
});
